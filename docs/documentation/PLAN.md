# Documentation plan

Decided after the full inventory (`APP-INVENTORY.md`) and claim audit
(`AUDIT.md`). Source of truth for every row: the working tree, `cargo test
--locked` (49 green), and the running app.

| File | Verdict | Reason |
|---|---|---|
| `README.md` | REWRITE | Feature list mostly accurate but no user installation path, no shortcut list, undocumented features (match case, Go To/wrap interaction, UTF-8-only, single-document, undo cap), Flatpak build steps incomplete (missing vendor step), no AI notice, no license section |
| `CONTRIBUTING.md` | CREATE | None existed. Short file: prerequisites, build/test/lint commands, repo layout, what the tests pin |
| `docs/ARCHITECTURE.md` | CREATE | Small contributor-oriented map: module list, state flow, config, packaging. Distilled from the verified parts of `docs/migration/architecture.md` + `DECISIONS.md` |
| `docs/migration/` (7 files, ~5,600 lines) | REMOVE | Agent-team process record of a finished migration; describes unlanded work as if live; references machines/checkouts that no longer exist. History preserves it |
| `plans/` (2 files) | REMOVE | Completed planning checklists for the rewrite; superseded by the code itself |
| `data/com.goshapps.Notepad.metainfo.xml` | KEEP | Verified accurate, including release history |
| `data/com.goshapps.Notepad.desktop` | KEEP | Verified |
| `docs/documentation/` (this dir) | CREATE | The audit artifacts of this pass: inventory, audit, plan |
| `LICENSE`, `COPYRIGHT` | KEEP | Required by tests and manifest |
| `justfile`, `i18n.toml`, `rust-toolchain.toml`, `.gitignore` | KEEP | Verified against actual use (`.cargo/` is gitignored — generated by `scripts/vendor.sh`) |
| `INSTALL.md` / `BUILDING.md` / `DEVELOPMENT.md` | not created | README + CONTRIBUTING cover this; splitting would duplicate |

## README outline (applied)

Name + one-line description → AI-development notice → feature list →
installation (remote caveat, Flatpak build, native) → usage & shortcuts →
limitations → development pointer → license.
